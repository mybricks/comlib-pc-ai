# 简介
- 建议使用的日期库：dayjs

## 如何引用
引用antd需要遵循此按需引用的方式
```javascript
import { Button } from 'antd'
```

## 何时使用
对于基础组件，比如图片、图标、按钮、文本，必须优先使用；
对于必要的组件，比如表单、Tabs、导航菜单、表格、进度条，建议按需使用；
对于仅为了便利性服务的组件，比如列表、卡片，仅在用户强调时使用；
